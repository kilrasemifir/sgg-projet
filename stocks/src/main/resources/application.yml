spring:
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL5Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/stocks?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
server:
  port: 8081